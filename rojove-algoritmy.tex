
Rojové algoritmy mají nìkteré spoleèné rysy s evoluèními algoritmy, jako je populaèní prohledávání, ale jsou charakteistické tím, ¾e kladou dùraz na modelování pohybu populace v prostoru parametrù inspirovaném rùznými druhy organizace sociálního hmyzu nebo jiných ¾ivoèichù v hejnech. Poèáteèní výzkum Reynoldse vedl k návrhu jednoduchého lokálního algoritmu tzv. boidù, kteøí velice jednodu¹e a pøekvapivì dobøe modelují organizaci roje a koordinovaný pohyb v nìm. Teprve algoritmus optimalizace rojem èástic (PSO) ale pøinesl vyu¾ití tìchto principù do prohledávacích algoritmù. V souèasnosti existuje celá øada dal¹ích pøibuzných algoritmù od ant colony optimization pøes umìlé imunitní systémy a¾ ke vèelím algoritmùm \cite{swarm}. 

Principem fungování rojovì optimalizace je organizovaný pohyb roje èástic (jedincù) po prostoru parametrù. Ka¾dá èástice je charakterizována svou polohou a vektorem rychlosti. Ka¾dá èastice si také pamatuje ze své historie nejlep¹í místo (ve smyslu hodnoty úèelové funkce), které ona sama v historii nav¹tívila a globální dosud nejlep¹í místo nalezené celým rojem. V ka¾dém kroku pak èástice upraví svou rychlost jako souèet tøi vektorù: souèasné rychlosti, smìru k lokálnimu extrému èastice a smìru ke globálnímu extrému celého roje. Tato suma je vá¾ena dílem vstupními parametry algoritmu nastavovanými experimentátorem (jde o nelehký problém závisející na typu dat) a dílem náhodnì vygenreovanými koeficienty pro ka¾dý krok. 

\begin{algorithm}
\caption{Schéma optimalizace rojem èástic minimalizujících $f:\Real^n\to\Real$}
\label{obrswarm}
\begin{algorithmic}
\Procedure{Optimalizace rojem èástic}{}
\State $\vec{g} \gets \arg\max_{\vec{y}} f(\vec{y})$ \Comment inicializace globální nejlep¹í pozice
\For{$i \gets 1, \dots, l$}
\State Inicializuj pozici èástice $\vec{x_i}$ náhodnì z rovnomìrného rozdìlení
\State $\vec{p_i} \gets \vec{x_i}$ \Comment inicializace nejlep¹í známé pozice èástice
\If{$f(\vec{p_i})<f(\vec{g})$}
\State $\vec{g} \gets \vec{p_i}$ \Comment update globální nejlep¹í pozice
\EndIf
\State Inicializuj rychlost èástice $\vec{v_i}$ náhodnì z rovnomìrného rozdìlení
\EndFor
\While{$\left(f(\vec{x^t})>\mbox{\emph{kritérium ukonèení}}\right)$}
\For{$i \gets 1, \dots, l$}
\State 	zvol $r_g$ a $r_p$ z ronvomìrného rozdìlení $U(0,1)$
\For{$d \gets 1, \dots, n$} \Comment update rychlosti èástice
\State $v_{i,d} \gets \omega v_{i,d} + \varphi_p r_p (p_{i,d} - x_{i,d}) + \varphi_g r_g (g_{i,d} - x_{i,d})$
\EndFor
\State $\vec{x_i} \gets \vec{x_i} + \vec{v_i}$ \Comment update pozice èástice
\If{$f(\vec{x_i})<f(\vec{p_i})$}
\State $\vec{p_i} \gets \vec{x_i}$ \Comment update nejlep¹í pozice èástice
\If{$f(\vec{p_i})<f(\vec{g})$}
\State $\vec{g} \gets \vec{p_i}$ \Comment update nejlep¹í globální pozice
\EndIf
\EndIf
\EndFor
\EndWhile
\EndProcedure
\end{algorithmic}
\end{algorithm}

