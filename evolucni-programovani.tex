Evoluèní programování je oblast, která je na rozdíl od genetických algoritmù charakterizována velmi benevolentním pøístupem ke kódování jedincù. První varianty tohoto pøístupu vznikly jako metoda pro automatickou evoluci koneèných automatù \cite{fogel}. Jedincem v populaci je v tomto pøipadì tedy koneèný automat (zakódovaný jakkoliv) a variaèní operace se omezují na nìkolik rùzných mutací. Pro evoluèní programování je charakteristické, ¾e mutace jsou pøizpùsobené problému a je jich èasto více, zatímco køí¾ení se vùbec nepou¾ívá. 

\subsection{EP na KA}

\subsection{EP na èíslech}

V dal¹ím textu popí¹eme variantu evoluèního programování, která pracuje s jedinci reprezentovanými vektory reálných èísel. Rodièovská selekce je jednoduchá, ka¾dý rodiè je jednou vybrán a mutací dá vznik jednomu potomkovi. Enviromentální selekce potom z mno¾iny rodièù a potomkù vybere turnajovou selekcí polovinu, která se stane dal¹í generací. 

\begin{algorithm}
\caption{Schéma meta-evoluèního programování nad vektorem reálných èisel}
\label{obrep}
\begin{algorithmic}
\Procedure{Meta-EP}{} 
\State $t \gets 0$
\State \emph{Inicializuj} populaci $P_t$ $N$ náhodnì vygenerovanými reálnými vektory $\vec{x^t}=(x_1^t,\dots,x_n^t,\sigma_1^t,\dots,\sigma_n^t)$
\State \emph{Ohodno»} jedince v populaci $P_t$
\While{neplatí \emph{kritérium ukonèení}}
\For{$i \gets 1, \dots, N$}
\State 	k rodièi $\vec{x_i^t}$ vygenruj potomka $\vec{y_i^t}$ \emph{mutací}:
\For{$j \gets 1, \dots, n$}
\State $\sigma'_j \gets \sigma_j \cdot (1+\alpha \cdot N(0,1))$
\State $x'_j \gets x_j + \sigma'_j \cdot N(0,1)$

\EndFor
\State vlo¾ $\vec{y_i^t}$ do kandidátské populace potomkù $P'_t$
\EndFor
\State 	\emph{Turnajovou selekcí} vyber $P_{t+1}$ z rodièù $P_t$ a potomkù $P'_t$
\State 	Zahoï $P_t$ a $P'_t$
\State $t \gets t+1$
\EndWhile
\EndProcedure
\end{algorithmic}
\end{algorithm}
 
Mutace v na¹em pøipadì pracuje tak, ¾e se sna¾í o malou zmìnu reálné hodnoty, kterou realizuje výbìrem z normálního rozdìlení. Ukázalo se, ¾e hodnoty rozptylu normálního rozdìlení jednotlivých parametrù jsou podstatné pro dobré fungování algoritmu. Jedno z navr¾ených øe¹ení je zaèlenit tyto rozptyly do ka¾dého jedince a upravovat je také v prùbìhu algoritmu. Jeliko¾ tak vlastnì upravujeme parametry, které zároveò pou¾íváme pøi vlastní mutaci, hovoøíme o meta-evoluci.

\subsection{Význam køí¾ení pro EA}
