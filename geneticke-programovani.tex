Gentické programování je zajímavé tím, ¾e pøiná¹í práci s jedinci odli¹ného typu --- jde o stromy reprezentující programy \cite{koza}. Strom je sestaven z mno¾iny neterminálních symbolù reprezentujících funkce a mno¾iny terminálních symbolù (listù) reprezentujících promìnné a konstanty. Promìnné jsou charakterizovány úlohou k øe¹ení, neterminální symboly urèují programovací jazyk, ve kterém budou programy vznikat. Stromy jsou vhodné jednak proto, ¾e pøedstavují kompaktní reprezentaci programù, a také proto, ¾e na nich lze elegantnì provádìt køí¾ení realizované jako výmìna náhodnì zvolených podstromù mezi dvìma jedinci. Mutace je realizována jako nahrazení podstromu náhodnì vygenerovaným podstromem. Je zajímavé, ¾e v kontrastu s jinými oblastmi evoluèních algoritmù, nehraje mutace v genetickém programování tak významnou roli a obvykle mívá malou pravdìpodobnost. Je to pravdìpodobnì zpùsobeno tím, ¾e køí¾ení pøedstavuje ve stromu programu velké zmìny, které tak suplují i úèinek mutace.

Náhodné generování stromù je dùle¾itou operací jak pro inicializaci populace tak pro mutace. Byly navr¾eny dvì metody, které se sna¾í buï náhodnì generovat kompletní strom dané hloubky, anebo náhodnì rostou vìtve stromù. Druhá metoda generuje tedy øid¹í stromy s nestejnì dlouhými vìtvemi. Pro inicializaci se doporuèuje kombinace tìchto metod v pomìru 1:1 (ramped half-and-half).

\begin{algorithm}
\caption{Schéma Kozova algoritmu genetického programování nad syntaktickými stromy}
\label{obrsgp}
\begin{algorithmic}
\Procedure{Genetické programování}{} 
\State $t \gets 0$
\State \emph{Inicializuj} populaci $P_t$ metodou \emph{ramped half-and-half}
\State \emph{Ohodno»} jedince v populaci $P_t$
\While{neplatí \emph{kritérium ukonèení}}
\State $i \gets 0$
\Repeat
\State vyber pravdìpodobnostnì variaèní operaci $o\in\{C,M\}$
\If{o=C} \Comment Køí¾ení
\State vyber z $P_t$ 2 rodièe
\State \emph{Zkøi¾} rodièe
\State vlo¾ potomky do $P_{t+1}$
\State $i \gets i + 2$
\Else \Comment Mutace
\State vyber z $P_t$ 1 rodièe
\State \emph{Mutuj} rodièe
\State vlo¾ potomka do $P_{t+1}$
\State $i \gets i + 1$
\EndIf
\Until{$i\geq n$}
\State \emph{Ohodno»} jedince v populaci $P_{t+1}$
\State 	Zahoï $P_t$
\State $t \gets t+1$
\EndWhile
%\Until{kr
\EndProcedure
\end{algorithmic}
\end{algorithm}

